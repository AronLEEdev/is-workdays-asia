# TODO: Document required env vars — DATABASE_URL (PostgreSQL), REDIS_URL (rate limiting),
# API_KEY_SECRET (for signing keys), Stripe keys when billing is added. See PRD §5–7.

# DATABASE_URL=postgresql://user:password@localhost:5432/isworkday
# REDIS_URL=redis://localhost:6379
# API_KEY_SECRET=your-secret-for-signing
