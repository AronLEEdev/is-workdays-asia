// TODO: GET /v1/next — Query params: country, date, skip (default 1). Return from, next_working_day,
// working_days_skipped, country. Find the Nth working day after the given date from calendar_entries.
// Use auth + rateLimit middleware.
